<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/brand2</item>
        <item name="colorPrimaryDark">@color/brand2</item>
        <item name="colorAccent">@color/brand2</item>
    </style>
    <declare-styleable name="BgColor" >
        <attr name="backgroundColor" format="color" />
        <attr name="itemFillColor" format="color" />
        <attr name="rotate" format="boolean" />
    </declare-styleable>

</resources>
